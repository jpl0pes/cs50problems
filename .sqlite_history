.help
.schema
.exit
.schema
CREATE TABLE IF NOT EXISTS 'users' (
'tx_id' INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, 
'user_id' TEXT NOT NULL, 
'stock_symbol' TEXT NOT NULL,
'stock_price' NUMERIC NOT NULL, 
'stock_qty' NUMERIC NOT NULL);
CREATE TABLE IF NOT EXISTS 'transactions' (
'tx_id' INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
'user_id' TEXT NOT NULL,
'stock_symbol' TEXT NOT NULL,
'stock_price' NUMERIC NOT NULL,
'stock_qty' NUMERIC NOT NULL);
exit
.exit
exit()
.exit;
.exit
ALTER TABLE Customers
;
.schema
ALTER TABLE transactions
    ADD name varchar(255) not null;
ALTER TABLE transactions
ADD stock_name TEXT NOT NULL;
ALTER TABLE transactions
ADD stock_name TEXT;
.schema
DROP TABLE transactions
.schema
;
DROP TABLE transactions;
.schema
CREATE TABLE IF NOT EXISTS 'transactions' (
'tx_id' INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
'user_id' TEXT NOT NULL,
'stock_symbol' TEXT NOT NULL,
'stock_name' TEXT NOT NULL,
'stock_price' NUMERIC NOT NULL,
'stock_qty' NUMERIC NOT NULL, stock_name TEXT);
.schema
CREATE TABLE IF NOT EXISTS 'transactions' (
'tx_id' INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
'user_id' TEXT NOT NULL,
'stock_symbol' TEXT NOT NULL,
'stock_name' TEXT NOT NULL,
'stock_price' NUMERIC NOT NULL,
'stock_qty' NUMERIC NOT NULL, stock_name TEXT);
CREATE TABLE IF NOT EXISTS 'transactions' (
'tx_id' INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
'user_id' TEXT NOT NULL,
'stock_symbol' TEXT NOT NULL,
'stock_name' TEXT NOT NULL,
'stock_price' NUMERIC NOT NULL,
'stock_qty' NUMERIC NOT NULL);
.schema
exit
;
exit
;
.exit
SELECT sum(stock_qty) FROM transactions WHERE user_id = 4 AND stock_symbol = 'AAPL')
;
SELECT sum(stock_qty) FROM transactions WHERE user_id = 4 AND stock_symbol = 'AAPL';
.exit
SELECT DISTINCT(stock_name) as 'name' FROM transactions WHERE user_id = 4 AND stock_symbol ="AAPL";
.exit
SELECT sum(stock_qty) as 'current_shares' FROM transactions WHERE user_id = :user_id AND stock_symbol= :stock_symbol;
SELECT sum(stock_qty) as 'current_shares' FROM transactions WHERE user_id = 4 AND stock_symbol= :stock_symbol;
SELECT sum(stock_qty) as 'current_shares' FROM transactions WHERE user_id = :user_id AND stock_symbol= "APPL"
;
SELECT * FROM transactions
;
.schema
;
.schema
;
schema
exit
.exit
;
.exit
.schema
DROP transactions;#
;
DROP transactions;
DROPTABLE transactions;
DROP TABLE transactions;
.schema
CREATE TABLE IF NOT EXISTS 'transactions' (
'timestamp' DATETIME DEFAULT CURRENT_TIMESTAMP,
'tx_id' INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
'user_id' TEXT NOT NULL,
'stock_symbol' TEXT NOT NULL,
'stock_name' TEXT NOT NULL,
'stock_price' NUMERIC NOT NULL,
'stock_qty' NUMERIC NOT NULL);
.schema
.exit
SELECT stock_symbol, stock_qty, stock_price, timestamp FROM transactions WHERE user_id = 4
;
exit
.exti;
.exit
